<% if user_signed_in? && current_user.id == comment.user_id %>
  <%= link_to post_comment_path(comment.post_id, comment.id), method: :delete, remote: true, class: "comment-info__delete", data: { confirm: "削除しますか？" } do %> 
    <i class="far fa-trash-alt"></i>
  <% end %>
<% end %>