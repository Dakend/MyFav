<% if user_signed_in? && current_user.id == post.user_id %>
  <%= link_to post_path(post), method: :delete, class: "post-show-content-head__info-delete-logo", data: { confirm: "削除しますか？" } do %> 
    <i class="far fa-trash-alt"></i>
  <% end %>
<% end %>