<div class="info-action__favorite-logo" id="post-favorite-logo-<%= post.id %>">
  <% if user_signed_in? && !(current_user.id == post.user_id) %>
    <% if post.is_favorited_by?(current_user) %> 
      <%= render 'posts/favorited', post: post %>
    <% else %>
      <%= render 'posts/non_favorited', post: post %>
    <% end %>
  <% else %>
    <div class="info-action__favorite-logo-marked">
      <i class="fas fa-heart fa-2x"></i>
    </div>
  <% end %>
</div>
<div class="info-action__favorite-number" id="post-favorite-number-<%= post.id %>"><%= post.favorites.count %></div>